{
  "build": {
    "builder": "RAILPACK",
    "buildpacks": ["heroku/nodejs"]
  },
  "deploy": {
    "startCommand": "cd backend && npm install && npm start"
  },
  "env": {
    "NODE_VERSION": "20.18.1"
  }
}
